<style>
  html {
    background:
    repeating-conic-gradient({{ section.settings.color_secondary }} 0 90deg, {{ section.settings.color_primary }} 0 180deg) 0 60px / calc(100vw - calc(100vw - 100%)) 100vw;
  }

  @media (min-width: 990px) {
    html {
      background:
      repeating-conic-gradient({{ section.settings.color_secondary }} 0 90deg, {{ section.settings.color_primary }} 0 180deg) 0 60px / calc(50vw - calc(50vw - 50%)) 50vw;
    }
  }
</style>

{% schema %}
{
  "name": "Checkered Background",
  "settings": [
    {
      "type": "color",
      "id": "color_primary",
      "label": "Primary",
      "default": "#FFFFFF"
    },
    {
      "type": "color",
      "id": "color_secondary",
      "label": "Secondary",
      "default": "#F6F6F6"
    }
  ],
  "presets": [
    {
      "name": "Checkered Background",
      "settings": {
      }
    }
  ]
}
{% endschema %}
